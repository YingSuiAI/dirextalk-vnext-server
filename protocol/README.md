# Protocol Artifacts

This directory will contain the canonical CDDL, OpenAPI, Protobuf, and cross-language test vectors introduced by S0.3.

S0.1 defines only in-process Rust compatibility primitives in `dtx-wire`. No wire encoding is frozen yet, and these Rust types must not be serialized as a public protocol until S0.3 is complete.
